package com.select_food.main.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.select_food.main.model.ResponseComponent;

public interface ResponseRepository extends JpaRepository<ResponseComponent,Integer>{
    
}
